# Elasticsearch Log4j2 minimal configuration to log only errors
# Root logger level (affects most ES categories)
rootLogger.level = error

# Deprecation logger (set to error to suppress warnings)
logger.deprecation.name = org.elasticsearch.deprecation
logger.deprecation.level = error
