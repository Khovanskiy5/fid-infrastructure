# Specify here the failover parameters that will
# be used by the `cartridge failover setup` command

mode: stateful
state_provider: stateboard
stateboard_params:
  uri: 0.0.0.0:4401
  password: passwd
